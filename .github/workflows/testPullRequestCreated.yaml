name: Pull request created
# This workflow is triggered when a pull request is created
on: [pull_request]

jobs:
  build:
    # Job name is Greeting
    name: Greeting
    # This job runs on Linux
    runs-on: ubuntu-latest
    steps:
      - name: Tell that this is a pull request evebt
        run: echo 'Pull request created'